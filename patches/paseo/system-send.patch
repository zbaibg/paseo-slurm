From local/paseo-v0.8.0-companion Mon Sep 17 00:00:00 2001
From: zbai29 <zbai29@wisc.edu>
Date: Fri, 11 Sep 2026 00:00:00 +0000
Subject: [PATCH] Add paseo send --system for external-wait resumes

Incremental on top of the finish-deferral companion patch. Vanilla checkouts
should apply external-wait-finish-deferral.patch instead (it already includes
this).

Rebased onto getpaseo/paseo v0.8.0 (e432c9b47) plus finish deferral.

Co-authored-by: Cursor <cursoragent@cursor.com>
---
diff --git a/docs/agent-lifecycle.md b/docs/agent-lifecycle.md
index b99a106fd..103388107 100644
--- a/docs/agent-lifecycle.md
+++ b/docs/agent-lifecycle.md
@@ -93,6 +93,9 @@ The label marks an external controller's ownership; Paseo does not interpret the
 the external system, group membership, or completion strategy. The controller must serialize its
 state transitions and completion prompts. Errors and permission requests still notify the caller
 immediately. Clearing the label while the child is idle does not itself complete the delegated task.
+External controllers should resume the child with `paseo send --system`; this uses the same hidden
+system-context path as child-to-parent finish notifications instead of adding a user-message bubble
+to the timeline.
 Permission requests are notification checkpoints, not the end of that subscription. The caller is notified again after a permission response when the child finishes, errors, or requests another permission.
 The permission notification includes the normalized request plus the child and request IDs, so the caller can inspect it and respond without fetching agent status.
 A watched child that closes before its finish event also notifies the caller so delegated work cannot disappear silently during archive or workspace teardown.
diff --git a/packages/cli/src/commands/agent/send.ts b/packages/cli/src/commands/agent/send.ts
index 06249289b..39949f062 100644
--- a/packages/cli/src/commands/agent/send.ts
+++ b/packages/cli/src/commands/agent/send.ts
@@ -32,6 +32,7 @@ export interface AgentSendOptions extends CommandOptions {
   image?: string[];
   prompt?: string;
   promptFile?: string;
+  system?: boolean;
 }
 
 export function addSendOptions(cmd: Command): Command {
@@ -41,6 +42,7 @@ export function addSendOptions(cmd: Command): Command {
     .argument("[prompt]", "The message to send")
     .option("--prompt <text>", "Provide the message inline as a flag")
     .option("--prompt-file <path>", "Read the message from a UTF-8 text file")
+    .option("--system", "Send as internal system context without a user-message bubble")
     .option("--image <path>", "Attach image(s) to the message", collectMultiple, [])
     .option("--no-wait", "Return immediately without waiting for completion");
 }
@@ -203,7 +205,7 @@ export async function runSendCommand(
       options.image && options.image.length > 0 ? await readImageFiles(options.image) : undefined;
 
     // Send the message
-    await client.sendAgentMessage(agentIdArg, promptInput, { images });
+    await client.sendAgentMessage(agentIdArg, promptInput, { images, system: options.system });
 
     // If --no-wait, return immediately
     if (options.wait === false) {
diff --git a/packages/client/src/daemon-client.ts b/packages/client/src/daemon-client.ts
index e22534da3..06dd4d6c7 100644
--- a/packages/client/src/daemon-client.ts
+++ b/packages/client/src/daemon-client.ts
@@ -353,6 +353,7 @@ export interface DaemonClientTrace {
 
 export interface SendMessageOptions {
   messageId?: string;
+  system?: boolean;
   activeTurnBehavior?: ActiveTurnBehavior;
   images?: Array<{ data: string; mimeType: string }>;
   attachments?: SendAgentMessageRequest["attachments"];
@@ -3192,13 +3193,14 @@ export class DaemonClient {
     options?: SendMessageOptions,
   ): Promise<void> {
     const requestId = this.createRequestId();
-    const messageId = options?.messageId ?? crypto.randomUUID();
+    const messageId = options?.system ? undefined : (options?.messageId ?? crypto.randomUUID());
     const message = SessionInboundMessageSchema.parse({
       type: "send_agent_message_request",
       requestId,
       agentId,
       text,
       ...(messageId ? { messageId } : {}),
+      ...(options?.system ? { system: true } : {}),
       ...(options?.activeTurnBehavior ? { activeTurnBehavior: options.activeTurnBehavior } : {}),
       ...(options?.images ? { images: options.images } : {}),
       ...(options?.attachments ? { attachments: options.attachments } : {}),
diff --git a/packages/client/src/index.test.ts b/packages/client/src/index.test.ts
index 7cf75b580..bae088833 100644
--- a/packages/client/src/index.test.ts
+++ b/packages/client/src/index.test.ts
@@ -723,6 +723,28 @@ test("agent handles delegate create, send, timeline refetch, archive, and local
   );
   await sendPromise;
 
+  const systemSendPromise = agent.send("external work completed", { system: true });
+  const systemSendRequest = parseSentSessionMessage(ws.sent.at(-1));
+  expect(systemSendRequest).toMatchObject({
+    type: "send_agent_message_request",
+    agentId: "agent_sdk",
+    text: "external work completed",
+    system: true,
+  });
+  expect(systemSendRequest).not.toHaveProperty("messageId");
+  ws.message(
+    sessionMessage({
+      type: "send_agent_message_response",
+      payload: {
+        requestId: systemSendRequest.requestId,
+        agentId: "agent_sdk",
+        accepted: true,
+        error: null,
+      },
+    }),
+  );
+  await systemSendPromise;
+
   const runPromise = agent.run("finish the task", {
     messageId: "run-message-sdk",
     timeoutMs: 30_000,
diff --git a/packages/client/src/index.ts b/packages/client/src/index.ts
index 7068cdc0b..73c308a08 100644
--- a/packages/client/src/index.ts
+++ b/packages/client/src/index.ts
@@ -256,6 +256,7 @@ export interface PaseoAgentTimelineRefetchOptions {
 
 export interface PaseoAgentSendOptions {
   messageId?: string;
+  system?: boolean;
   images?: Array<{ data: string; mimeType: string }>;
   attachments?: SendAgentMessageRequest["attachments"];
 }
diff --git a/packages/protocol/src/messages.test.ts b/packages/protocol/src/messages.test.ts
index 3394c8e15..95c5368f7 100644
--- a/packages/protocol/src/messages.test.ts
+++ b/packages/protocol/src/messages.test.ts
@@ -76,6 +76,22 @@ describe("project icon revision compatibility", () => {
   });
 });
 
+describe("agent system messages", () => {
+  test("accepts a system-injected agent message request", () => {
+    const parsed = SessionInboundMessageSchema.parse({
+      type: "send_agent_message_request",
+      requestId: "request-system-1",
+      agentId: "agent-1",
+      text: "External work completed.",
+      system: true,
+    });
+    expect(parsed).toMatchObject({
+      type: "send_agent_message_request",
+      system: true,
+    });
+  });
+});
+
 describe("workspace descriptor message compatibility", () => {
   test("old-shaped fetch_workspaces_response without project still parses", () => {
     const parsed = SessionOutboundMessageSchema.parse(
diff --git a/packages/protocol/src/messages.ts b/packages/protocol/src/messages.ts
index ed36eeb1a..664adb305 100644
--- a/packages/protocol/src/messages.ts
+++ b/packages/protocol/src/messages.ts
@@ -1245,6 +1245,7 @@ export const SendAgentMessageSchema = z.object({
   agentId: z.string(),
   text: z.string(),
   messageId: z.string().optional(), // Client-provided ID for deduplication
+  system: z.boolean().optional(),
   activeTurnBehavior: ActiveTurnBehaviorSchema.optional(),
   images: z.array(ImageAttachmentSchema).optional(),
   attachments: AgentAttachmentsSchema,
@@ -1399,6 +1400,7 @@ export const SendAgentMessageRequestSchema = z.object({
   agentId: z.string(),
   text: z.string(),
   messageId: z.string().optional(), // Client-provided ID for deduplication
+  system: z.boolean().optional(),
   activeTurnBehavior: ActiveTurnBehaviorSchema.optional(),
   images: z.array(ImageAttachmentSchema).optional(),
   attachments: AgentAttachmentsSchema,
diff --git a/packages/server/src/server/session.ts b/packages/server/src/server/session.ts
index c1e149ee5..c484112bb 100644
--- a/packages/server/src/server/session.ts
+++ b/packages/server/src/server/session.ts
@@ -44,6 +44,8 @@ import {
 } from "./persistence-hooks.js";
 import { ensureAgentLoaded, ensureUnarchivedAgentLoaded } from "./agent/agent-loading.js";
 import {
+  formatSystemNotificationPrompt,
+  isSystemInjectedEnvelope,
   sendPromptToAgent,
   waitForAgentRunStartWithTimeout,
   unarchiveAgentState,
@@ -7587,11 +7589,15 @@ export class Session {
     try {
       const agentId = resolved.agentId;
 
-      const prompt = buildAgentPrompt(msg.text, msg.images, msg.attachments);
+      const text =
+        msg.system && !isSystemInjectedEnvelope(msg.text)
+          ? formatSystemNotificationPrompt(msg.text)
+          : msg.text;
+      const prompt = buildAgentPrompt(text, msg.images, msg.attachments);
       this.sessionLogger.trace(
         {
           agentId,
-          messageId: msg.messageId,
+          messageId: msg.system ? undefined : msg.messageId,
           activeTurnBehavior: msg.activeTurnBehavior,
           textPrefix: msg.text.slice(0, 80),
         },
@@ -7603,7 +7609,7 @@ export class Session {
           agentStorage: this.agentStorage,
           agentId,
           prompt,
-          messageId: msg.messageId,
+          messageId: msg.system ? undefined : msg.messageId,
           activeTurnBehavior: msg.activeTurnBehavior ?? "interrupt",
           clearPendingPermissions: true,
           logger: this.sessionLogger,
